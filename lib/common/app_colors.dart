import 'package:flutter/material.dart';

/// Contains all colors used in the application
class AppColors {
  static const Color primary = Color.fromRGBO(33, 150, 243, 1);
  static const Color secondary = Color.fromRGBO(156, 39, 176, 1);
  static const Color background = Color.fromRGBO(236, 240, 243, 1);

  /// Default Colors
  static const Color white = Colors.white;
  static const Color gray = Colors.grey;
  static const Color black = Colors.black;
  static Color shimmerBase = Colors.grey[300]!;
  static Color shimmerHl = Colors.grey[100]!;
  static const Color pink = Colors.pink;
  static const Color red = Colors.red;
  static const Color colorPrimary = Colors.pink;

  static var colorGreen = Colors.green;
}
